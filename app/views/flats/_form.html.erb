<%= simple_form_for(flat, html: { class: "form-width" }) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price_per_night %>
  <%= f.input :address %>
  <div class="btn-right">
    <%= f.submit class: "btn btn-blue" %>
  </div>
<% end %>
